<% users.each do |user| %>
  <div id="<%= user.id %>">
    <div class="user-name"><%= user.name %></div>
    <button type="button" class="btn btn-primary add-user-btn"
      id="button-add-<%= user.id %>">add</button>
  </div>
<% end %>
